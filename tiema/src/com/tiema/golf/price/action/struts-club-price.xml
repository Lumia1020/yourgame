<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE struts PUBLIC
        "-//Apache Software Foundation//DTD Struts Configuration 2.0//EN"
        "http://struts.apache.org/dtds/struts-2.0.dtd">

<struts>

	<package name="default-club-price" namespace="/default/club-price" extends="struts-default">
		<action name="*">
			<result>/module/{1}/{1}_manage.jsp</result>
		</action>
	</package>

	<package name="json-club-price" namespace="/json/club-price" extends="json-default">

		<action name="findAllByGolfClubId" class="clubPriceAction" method="findAllByGolfClubId">
			<result type="json">
				<param name="enumAsBean">true</param>
				<param name="includeProperties"><![CDATA[success,clubPrices.*]]></param>
			</result>
		</action>
		<action name="page!*" class="clubPriceAction">
			<result type="json">
				<param name="enumAsBean">true</param>
				<param name="root">page</param>
				<param name="includeProperties"><![CDATA[
					result.*,
					totalCount,
					success
				]]></param>
			</result>
		</action>
		<action name="success!*" class="clubPriceAction">
			<result type="json">
				<param name="includeProperties">success,jsons.*</param>
			</result>
		</action>


		<action name="entity-*" class="clubPriceAction" method="{1}">
			<interceptor-ref name="jsonValidationWorkflowStack" />
			<result type="json">
				<param name="enumAsBean">true</param>
				<param name="includeProperties">clubPrice\..*,success,jsons.*</param>
			</result>
		</action>
	</package>
</struts>
